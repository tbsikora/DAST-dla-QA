# API server port
PORT=4000

# Scanner HTTP trace logs in backend console (0/1)
DAST_HTTP_TRACE=0

# Capture request/response details in scan results (0/1)
# Default behavior in code is enabled unless set to 0.
DAST_HTTP_DEBUG_CAPTURE=1

# Optional explicit DB file path (JSON history storage)
# DAST_DB_FILE=apps/api/data/scans.json

