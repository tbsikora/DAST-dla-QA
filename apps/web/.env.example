# For local dev outside Docker.
# Vite dev server already proxies /api to http://localhost:4000, so this can stay empty.
# Set only when frontend and backend are on different origins.
# VITE_API_URL=http://localhost:4000

